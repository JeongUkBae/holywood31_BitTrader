package enums;

import command.Command;

public enum MoveController {
	/*
	 * Move;
	 * 
	 * private MoveCommand MoveController;
	 * 
	 * private MoveController(MoveCommand move) { this.MoveController = new
	 * MoveCommand(null, null); }
	 * 
	 * public MoveCommand getValue() { return MoveController; }
	 */
	
}
